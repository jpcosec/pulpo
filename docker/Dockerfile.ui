FROM node:20-alpine

WORKDIR /app

# Copy generated frontend
COPY run_cache/generated_frontend/ .

# Install dependencies with legacy peer deps flag
RUN npm install --legacy-peer-deps

# Expose port
EXPOSE 3000

# Run dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
